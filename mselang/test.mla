program test;
uses

type
{$internaldebug on}
 rangety = 1..1110;
 set1ty = set of rangety;
var
 s1: set1ty;
begin
 include(s1,2);
 writeln(int32(s1));
 if 2 in s1 then begin
  exitcode:= 123;
 end;
end.