program test;
uses
 unit1;
type

const
{$internaldebug on}
var
 set1: set of char8;
 s1: string8;
 ch1: char8;
begin
// s1:= stringconst+'x';
 set1:= setconst+['1'..'2'];
 for ch1:= low(ch1) to high(ch1) do
  if ch1 in set1 then
   write(ch1);
  end;
 end;
end.